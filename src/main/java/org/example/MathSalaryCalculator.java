package org.example;

public class MathSalaryCalculator {
    public double calculateSalaryWithMathPower(double baseSalary, double exponent) {
        // Use Math.pow to calculate salary with Math power
        return Math.pow(baseSalary, exponent);
    }
}
